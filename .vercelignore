# Exclude the backend API server (not deployed to Vercel)
/artifacts/api-server/

# Exclude workspace-level shared libraries (not needed for web build)
/lib/

# Exclude scripts package
/scripts/

# Build artifacts
*.tsbuildinfo
/static-build/
/medspotter-vercel/
/medspotter-vercel.zip

# Replit internals
/.cache/
/.local/
